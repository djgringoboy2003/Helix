name: Build APK

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            functions/package-lock.json

      - name: Install app dependencies
        run: npm ci

      - name: Install Functions dependencies
        run: npm ci --prefix functions

      - name: Type-check
        run: npx tsc --noEmit

      - name: Lint
        run: npx eslint . --quiet

      - name: Run regression checks
        run: npm run test:regressions

      - name: Build Functions
        run: npm run build --prefix functions

  build:
    needs: validate
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      # Whether a signed APK was actually produced. The release job reads this
      # so a skipped build can never be followed by a publish.
      built: ${{ steps.secrets.outputs.available }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install dependencies
        run: npm ci

      # A repository without the signing secrets — any fork — cannot produce a
      # signed APK, and never could. Failing here made this workflow red on
      # every single push, which is worse than useless: it trained everyone to
      # ignore the Actions tab, and a real `verify` failure (the missing
      # `services/gcode`, 00821a8) sat unread underneath the noise. So a fork
      # now reports and skips, exactly as `android-baseline.yml` already does,
      # and a red run means something again.
      #
      # Upstream, where the secrets exist, is unaffected: every step below still
      # runs and the release still publishes.
      - name: Check release secrets
        id: secrets
        env:
          FIREBASE_CONFIG_B64: ${{ secrets.FIREBASE_CONFIG_B64 }}
          RELEASE_KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_B64 }}
          RELEASE_KEYSTORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          missing=""
          for name in \
            FIREBASE_CONFIG_B64 \
            RELEASE_KEYSTORE_B64 \
            RELEASE_KEYSTORE_PASSWORD \
            RELEASE_KEY_ALIAS \
            RELEASE_KEY_PASSWORD
          do
            if [ -z "${!name}" ]; then
              missing="$missing $name"
            fi
          done
          if [ -n "$missing" ]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Release signing secrets are not configured ($missing ). The signed APK build and the release were skipped; type-check, lint and regression checks still ran."
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Decode release keystore
        if: steps.secrets.outputs.available == 'true'
        env:
          RELEASE_KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_B64 }}
        run: printf '%s' "$RELEASE_KEYSTORE_B64" | base64 -d > android/app/release.keystore

      - name: Decode Firebase Android config
        if: steps.secrets.outputs.available == 'true'
        env:
          FIREBASE_CONFIG_B64: ${{ secrets.FIREBASE_CONFIG_B64 }}
        run: printf '%s' "$FIREBASE_CONFIG_B64" | base64 -d > android/app/google-services.json

      - name: Test and build release APK
        if: steps.secrets.outputs.available == 'true'
        env:
          HELIX_DISTRIBUTION: github
          NODE_ENV: production
          RELEASE_KEYSTORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          chmod +x android/gradlew
          cd android
          ./gradlew testReleaseUnitTest assembleRelease --no-daemon

      - name: Rename APK
        if: steps.secrets.outputs.available == 'true'
        run: cp android/app/build/outputs/apk/release/app-release.apk helix.apk

      - uses: actions/upload-artifact@v7
        if: steps.secrets.outputs.available == 'true'
        with:
          name: helix-apk
          path: helix.apk

  release:
    needs: build
    # A skipped build must never be followed by a publish. Previously this was
    # true only incidentally, because `build` failed; now that it can succeed
    # without producing an APK, the guard has to be explicit.
    if: github.ref == 'refs/heads/main' && needs.build.outputs.built == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          # Full history + tags are needed to build notes from the closest
          # rolling or versioned release.
          fetch-depth: 0

      - uses: actions/download-artifact@v8
        with:
          name: helix-apk

      - name: Build release notes
        run: |
          BASE_REF=""
          if git rev-parse -q --verify refs/tags/latest >/dev/null; then
            BASE_REF="latest"
          fi

          VERSION_TAG="$(git tag --list 'v[0-9]*' --sort=-version:refname | head -n 1)"
          if [ -n "$VERSION_TAG" ] && git merge-base --is-ancestor "$VERSION_TAG" HEAD; then
            if [ -z "$BASE_REF" ] || \
              [ "$(git rev-list --count "$VERSION_TAG"..HEAD)" -lt "$(git rev-list --count "$BASE_REF"..HEAD)" ]; then
              BASE_REF="$VERSION_TAG"
            fi
          fi

          if [ -n "$BASE_REF" ]; then
            RAW="$(git log "$BASE_REF"..HEAD --no-merges --pretty='- %s (%h)')"
          else
            RAW="$(git log -10 --no-merges --pretty='- %s (%h)')"
          fi

          # Hide internal commits (ci:, chore:, ...) from the public changelog;
          # git history keeps them. Also read by the in-app update popup.
          NOTES="$(printf '%s\n' "$RAW" | grep -Ev '^- (ci|chore|build|docs|test|refactor)(\([^)]*\))?:' || true)"
          [ -z "$NOTES" ] && NOTES="- Maintenance update (no user-facing changes)"
          {
            echo 'RELEASE_NOTES<<NOTES_EOF'
            echo "## What's changed"
            echo "$NOTES"
            echo ''
            # Full 40-char SHA required: the app's update check parses it
            # out of the release body (services/updateCheck.ts releaseCommit)
            echo "Build: ${GITHUB_SHA}"
            echo 'NOTES_EOF'
          } >> "$GITHUB_ENV"

      - name: Advance rolling release tag
        run: |
          git tag --force latest "$GITHUB_SHA"
          git push origin refs/tags/latest --force

      - name: Update "latest" release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: latest
          name: Helix (latest)
          body: ${{ env.RELEASE_NOTES }}
          files: helix.apk
          prerelease: false
