node_modules/
.expo/
dist/
dist-check/
web-build/
*.log
.DS_Store
expo-env.d.ts

# native projects — android/ is committed (custom slicer native code); ios still prebuild-only
ios/
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/app/release.keystore
android/keystore.properties
android/app/google-services.json
.omx/
.firecrawl/
*.apk
tools/
.omc/
functions/lib/

# secrets / generated content (snapmaker-u1-makerworld app work)
.env
.env.*
!.env.example
coverage/
tmp/
downloads/
models/
gcode/
# ...but `gcode/` above is unanchored, so it also matched the *source*
# directory `services/gcode/` and silently kept Phase 7's G-code review out of
# the repository — `00821a8` committed the tests and the card without the three
# modules they import. Re-include it explicitly.
!services/gcode/
